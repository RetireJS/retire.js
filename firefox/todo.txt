* Fix leak with page worker. The background window object is not GC'ed when worker.destroy() is called.
* Check if URL.path returns file name and not local path
* Privacy file
* Use shared retire.js
* Review

Notes:
On some browser profiles the button is not moved to the toolbar after install.
Workaround: Manually move it the toolbar